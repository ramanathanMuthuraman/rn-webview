/* eslint-disable no-use-before-define */
import React from 'react';
import {WebView} from 'react-native-webview';

const RNWebView = (props) => {
  return <WebView {...props} />;
};

export default RNWebView;
